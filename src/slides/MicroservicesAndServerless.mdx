import Layout from '../components/Layout'

# Microservice-based architecture is another thing happened in tandem with the frontend and API layers splitting.

<Appear>
  <h4>
    Backend teams began moving away from one large monolithic API toward
    much smaller services with more narrowly defined responsibilities.
  </h4>
  <h4>
    The concept of cloud functions was born, whereby simple functions
    doing just one thing and nothing more can be exposed as microweb API
    endpoints.
  </h4>
</Appear>

export default ({ children }) => (
  <Layout>
    { children }
  </Layout>
)

import Layout from '../components/Layout'

When these approaches to building websites and applications 
intersected, a new API ecosystem began to emerge.

Tools like Stripe, Disqus, and Algolia made payments, comments,
and search available directly from the frontend via API-driven
services.

export default ({ children }) => (
  <Layout>
    { children }
  </Layout>
)

import Layout from '../components/Layout'
import SimpsonMP4 from '../components/images/SimpsonMP4'

# What should be simple is actually pretty complex

<Split>
  <SimpsonMP4 />
</Split>

export default ({ children }) => (
  <Layout>
    { children }
  </Layout>
)
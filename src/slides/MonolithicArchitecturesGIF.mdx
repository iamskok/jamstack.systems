import Layout from '../components/Layout'
import Simpson from '../components/images/SimpsonMP4'

# What should be simple is actually pretty complex

<Split>
  <Simpson />
</Split>

export default ({ children }) => (
  <Layout>
    { children }
  </Layout>
)
import Layout from '../components/Layout'
import GIF from '../images/simple.gif'

# What should be simple is actually pretty complex

<Split>
  <img src={ GIF } width="100%" />
</Split>

export default ({ children }) => (
  <Layout>
    { children }
  </Layout>
)
import Layout from '../components/Layout'

⚡️But performance **isn’t dictated by speed alone**. Because monolithic apps are so large, 
it can be difficult to define architectural boundaries. The code is so interconnected 
that fixing a 🐛, updating a library, or changing a framework in one part of the app 
can break another part of it.

export default ({ children }) => (
  <Layout>
    { children }
  </Layout>
)

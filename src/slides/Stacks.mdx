import Layout from '../components/Layout'
import Stacks from '../components/images/Stacks'

<SplitBottom>
  <h1>LAMP vs MEAN vs JAM</h1>
  <Stacks />
</SplitBottom>

<Notes>
  Before, we used to talk about the operating system, the database,
  and the web server when describing our stack.
</Notes>

export default ({ children }) => <Layout title="Stacks comparison 📊">{ children }</Layout>

import Layout from '../components/Layout'
import Stacks from '../components/images/Stacks'

<Stacks />

<Notes>
  <p>
    Before, we used to talk about the operating system,
    the database, and the web server when describing 
    our stack
  </p>
</Notes>

export default ({ children }) => <Layout title="Stack comparison 📊">{ children }</Layout>

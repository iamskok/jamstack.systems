import Layout from '../components/Layout'

This brought aproper software architecture to the frontend workflow and
empowered frontend developers to iterate much faster on the user interface
(UI) and interaction layer.

export default ({ children }) => (
  <Layout>
    { children }
  </Layout>
)
 
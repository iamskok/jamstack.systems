import Layout from '../components/Layout'

# JAMstack

<Notes>
  Warning: many developers might be affended by this deck
  because I will be talking 💩 about Monolithic apps.
</Notes>

export default ({ children }) => <Layout>{ children }</Layout>
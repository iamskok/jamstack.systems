import Layout from '../components/Layout'

Web sites have traditionally been powered by ~~[WordPress](https://wordpress.org/)~~
monolithic architectures, with the frontend of the application tightly coupled to 
the backend.

export default ({ children }) => (
  <Layout>
    { children }
  </Layout>
)
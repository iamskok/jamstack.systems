import Layout from '../components/Layout'
import AppearText from '../components/AppearText'

<AppearText>
  * JavaScript in the browser as the runtime
</AppearText>
<Appear>
  <div>
    <AppearText>
      * Reusable HTTP APIs rather than app-specific databases
    </AppearText>
  </div>
  <div>
    <AppearText>
      * Prebuilt markup as the delivery mechanism
    </AppearText>
  </div>
</Appear>

<Notes>
  <p>
    With the emergence of new practices, the real
    architectural constraints became the following
  </p>

  <ul>
    <ol>JavaScript in the browser as the runtime</ol>
    <ol>Reusable HTTP APIs rather than app-specific databases</ol>
    <ol>Prebuilt markup as the deolvery mechanism</ol>
  </ul>
</Notes>

export default ({ children }) => <Layout title="The real architectural constraints 🏔">{ children }</Layout>

import Layout from '../components/Layout'

<h1>JavaScript in the browser as the runtime</h1>
<Appear>
  <h1>Reusable HTTP APIs rather than app-specific databases</h1>
  <h1>Prebuilt markup as the delivery mechanism</h1>
</Appear>

<Notes>
  <p>
    With the emergence of new practices, the real
    architectural constraints became the following
  </p>

  <ul>
    <li>JavaScript in the browser as the runtime</li>
    <li>Reusable HTTP APIs rather than app-specific databases</li>
    <li>Prebuilt markup as the delivery mechanism</li>
  </ul>
</Notes>

export default ({ children }) => (
  <Layout>
    { children }
  </Layout>
)

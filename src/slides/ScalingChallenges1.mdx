import Layout from '../components/Layout'

Because monolithic architecture requires the page view
to be generated for every visitor, infrastructure needs
to be scaled in anticipation of site traffic.

<Appear>
  <div>💸 Not only is that expensive, it’s also difficult to get right.</div>
</Appear>

export default ({ children }) => (
  <Layout>
    { children }
  </Layout>
)

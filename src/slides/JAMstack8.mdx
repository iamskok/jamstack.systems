import Layout from '../components/Layout'

Before, we used to talk about the operating system, the database, and the web server 
when describing our stack (such as the LAMP stack and MEAN stack)

![](/lamp-mean-jam.jpeg)

export default ({ children }) => (
  <Layout>
    { children }
  </Layout>
)

import Layout from '../components/Layout'

# All of the web has become something like a giant operating system.

<Appear>
  <p>
    We’re now seeing APIs exposed for anything from payments or subscriptions
    over advanced machine learning models running in massive cloud environments,
    to services that directly affect the physical world like shipping or
    ride-sharing services, and just about anything else you can imagine.
  </p>
</Appear>

export default ({ children }) => (
  <Layout>
    { children }
  </Layout>
)

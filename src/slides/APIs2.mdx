import Layout from '../components/Layout'

The first web APIs were meant to be consumed from **server-side** applications.

<Appear>
  <p>
    New standards like CORS, WebSocket, and PostMessage paired with OAuth2 and
    JSON Web Token (JWT) for authorization and stateless authentication, 
    suddenly made <b>any modern web API</b> reachable from any JavaScript client
    running in a browser.
  </p>
</Appear>

export default ({ children }) => (
  <Layout>
    { children }
  </Layout>
)

import Layout from '../components/Layout'
import GIF from '../images/simple.gif'

<Image
  alt='What should be simple'
  src={ GIF }
  size='cover'
  style={{
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'center',
    color: 'white',
  }}>
  <h1>What should be simple</h1>
</Image>

export default ({ children }) => (
  <Layout>
    { children }
  </Layout>
)

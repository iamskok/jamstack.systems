import Layout from '../components/Layout'

🐢 Monolithic apps need to generate and deliver HTML every time a new visitor arrives 
on the site. This significantly slows down page load time.

<Appear>
  <div>⏰ A single-second delay in load time can hurt the conversion of an ecommerce site by 7%.</div>
</Appear>

export default ({ children }) => (
  <Layout>
    { children }
  </Layout>
)

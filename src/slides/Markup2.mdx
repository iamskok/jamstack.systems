import Layout from '../components/Layout'

JAMstack approach is to prebuild all the markup up
front and serve it directly to the browser from a CDN ⚡️.

<Appear>
  <p>
    This process typically involves a static site generator
    like Gatsby or Jekyll and a frontend build tool like Webpack
    where content and templates are combined into HTML, 
    source files are transpiled or compiled into JavaScript, and
    CSS runs through preprocessors or postprocessors.
  </p>
</Appear>

export default ({ children }) => (
  <Layout>
    { children }
  </Layout>
)

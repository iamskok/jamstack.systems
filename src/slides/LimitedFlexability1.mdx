import Layout from '../components/Layout'

The freedom to do good work—to design the exact experiences we
want for our users is regularly compromised by complex monolithic applications

export default ({ children }) => (
  <Layout>
    { children }
  </Layout>
)

import Layout from '../components/Layout'
import Build from '../components/images/Build'

<Split>

  # Now go and build something awesome!

  <Build />
</Split>

export default ({ children }) => <Layout title="Summary 🤓">{ children }</Layout>
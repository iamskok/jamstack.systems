import Layout from '../components/Layout'
import ecosystem from '../images/jamstack-ecosystem.jpeg'

<img src={ ecosystem } />

export default ({ children }) => (
  <Layout>
    { children }
  </Layout>
)
